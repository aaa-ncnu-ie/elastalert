# Alert when the rate of events exceeds a threshold

# (Optional)
# Elasticsearch host
# es_host: elasticsearch.example.com

# (Optional)
# Elasticsearch port
# es_port: 14900

# (OptionaL) Connect with SSL to elasticsearch
#use_ssl: True

# (Optional) basic-auth username and password for elasticsearch
#es_username: someusername
#es_password: somepassword

# (Required)
# Rule name, must be unique
# Note that every rule can have its own elasticsearch host
es_host: 10.91.36.101

# The elasticsearch port
es_port: 9200
name: HTTP 50X Code Detected
# (Required)
# Type of alert.
# the frequency rule type alerts when num_events events occur with timeframe time
type: frequency

# (Required)
# Index to search, wildcard supported
index: logstash-*

# (Required, frequency specific)
# Alert when this many documents matching the query occur within a timeframe
num_events: 1

# (Required, frequency specific)
# num_events must occur within this amount of time to trigger an alert
timeframe:
  minutes: 5

# (Required)
# A list of elasticsearch filters used for find events
# These filters are joined with AND and nested in a filtered query
# For more info: http://www.elasticsearch.org/guide/en/elasticsearch/reference/current/query-dsl.html
filter:
- query_string:
    query: "response_code:[500 TO 511] AND source_host:ds.mypolicy"

# (Required)
# The alert is use when a match is found
alert:
- "slack"
# - "command"
#
# command: ["curl", "-XPOST", "-H \"Content-Type: application/json\"", "-d@-", "https://hooks.zapier.com/hooks/catch/1143336/14lke0"]
#
# pipe_match_json: true
#
# alert_text: "200 Detected on mypolicy {0}"
# alert_text_type: alert_text_only
# alert_text_args: ["source_host"]

slack_webhook_url: "https://hooks.slack.com/services/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"




#smtp_host: smtp.gmail.com
#smtp_port: 46
#smtp_auth_file: smtp_auth.yml
#smtp_ssl: true
#from_addr: shaozhen.ding@gmail.com

# (required, email specific)
# a list of email addresses to send alerts to
#email:
#- "sding@pivotal.io"
